{# templates/persona/show.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}Persona Details{% endblock %}

{% block body %}
    <h1>{{ persona.nombre }} {{ persona.apellidos }}</h1>

    <h2>Quinquenio Solicitudes</h2>
    <table class="table table-striped">
        <thead>
            <tr>
                <th>ID</th>
                <th>Fecha Entrada</th>
            </tr>
        </thead>
        <tbody>
            {% for solicitud in quinquenioSolicitudes %}
                <tr>
                    <td>{{ solicitud.id }}</td>
                    <td>{{ solicitud.fechaEntrada|date('Y-m-d H:i') }}</td>
                    <td>
                        <a href="{{ path('quinquenio_solicitud_show', {id: solicitud.id}) }}" class="btn btn-info">Ver Méritos</a>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('persona_index') }}" class="btn btn-secondary">Back to list</a>
{% endblock %}
