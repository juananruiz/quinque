{% extends 'base.html.twig' %}

{% block title %}Editar datos solicitante{% endblock %}

{% block body %}
    <div class="container">
        <div class="row justify-content-center">
            <h1 class="text-center mb-4"><i class="fas fa-user-edit"></i>
                <span class="d-none d-sm-inline">Editar datos solicitante</span></h1>
            <div class="col-md-8 col-lg-6">
                {{ include('persona/_form.html.twig', {'button_label': 'Actualizar'}) }}
                
                <div class="text-end">
                    <a href="{{ path('persona_index') }}" class="btn btn-secondary">Volver al listado</a>
                </div>
                
                {{ include('persona/_delete_form.html.twig') }}

            </div>
        </div>
    </div>
{% endblock %}
