{# templates/intranet/quinque/admin/departamento/new.html.twig #}
{% extends 'intranet/quinque/admin/base.html.twig' %}

{% block title %}Agregar Departamento{% endblock %}

{% block body %}
    <div class="row justify-content-center">
        <h1 class="mt-3 col-md-8"><i class="fas fa-building text-warning"></i> Agregar Departamento</h1>
        <div class="container mt-3 col-md-8">
            {{ form_start(form, {'attr': {'class': 'needs-validation'}}) }}
                {{ form_widget(form) }}
                <button class="btn btn-primary mt-3">Guardar</button>
            {{ form_end(form) }}
            <div class="text-end mb-3">
                <a href="{{ path('quinque_departamento_index') }}" class="btn btn-secondary"><i class="fas fa-arrow-left"></i> Volver</a>
            </div>
        </div>
    </div>
{% endblock %}