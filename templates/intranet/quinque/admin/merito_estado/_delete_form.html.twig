{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-danger" onclick="return confirm('¿Estás seguro de que quieres eliminar este elemento?');">
        <i class="fas fa-trash"></i> Eliminar
    </button>
{{ form_end(form) }}
