{% extends 'base.html.twig' %}

{% block title %}Estado de Solicitud{% endblock %}

{% block body %}
    <div class="container-fluid">
        <h1>Estado de Solicitud</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ solicitud_estado.id }}</td>
                </tr>
                <tr>
                    <th>Nombre</th>
                    <td>{{ solicitud_estado.nombre }}</td>
                </tr>
                <tr>
                    <th>Color</th>
                    <td>
                        <span class="badge" style="background-color: {{ solicitud_estado.color }}">
                            {{ solicitud_estado.color }}
                        </span>
                    </td>
                </tr>
            </tbody>
        </table>

        <div class="mt-3">
            <a href="{{ path('quinque_solicitud_estado_index') }}" class="btn btn-secondary">
                <i class="fas fa-arrow-left"></i> Volver al listado
            </a>

            <a href="{{ path('quinque_solicitud_estado_edit', {'id': solicitud_estado.id}) }}" class="btn btn-primary">
                <i class="fas fa-edit"></i> Editar
            </a>

            {{ include('intranet/quinque/admin/solicitud_estado/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
