<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Solicitud</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            margin: 20px;
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
        }
        .section {
            margin-bottom: 20px;
        }
        .section-title {
            font-weight: bold;
            border-bottom: 1px solid #ccc;
            margin-bottom: 10px;
        }
        .field {
            margin-bottom: 10px;
        }
        .field-label {
            font-weight: bold;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #f5f5f5;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>Solicitud de Méritos</h1>
    </div>

    <div class="section">
        <div class="section-title">Datos del Solicitante</div>
        <div class="field">
            <span class="field-label">Nombre:</span>
            {{ solicitud.persona.nombre }} {{ solicitud.persona.apellidos }}
        </div>
        <div class="field">
            <span class="field-label">DNI:</span>
            {{ solicitud.persona.dni }}
        </div>
    </div>

    <div class="section">
        <div class="section-title">Méritos Presentados</div>
        <table>
            <thead>
                <tr>
                    <th>Categoría</th>
                    <th>Estado</th>
                    <th>Puntuación</th>
                </tr>
            </thead>
            <tbody>
                {% for merito in solicitud.meritos %}
                <tr>
                    <td>{{ merito.categoria.nombre }}</td>
                    <td>{{ merito.estado.nombre }}</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>

    <div class="section">
        <div class="section-title">Resumen</div>
        <div class="field">
            <span class="field-label">Total Méritos:</span>
            {{ solicitud.meritos|length }}
        </div>
        <div class="field">
            <span class="field-label">Puntuación Total:</span>
            {{ solicitud.meritosComputados }}
        </div>
    </div>
</body>
</html>
